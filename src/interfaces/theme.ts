export type Theme = {
  colors: {
    background: string;
    backgroundSecondary: string;
    text: string;
    textSecondary: string;
    primary: string;
  };
  table: {
    background: string;
    text: string;
    danger: string;
  };
};
